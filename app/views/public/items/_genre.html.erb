<table class="table-borderless">
  <tr>
    <th class="font-weight-bold text-center pl-4 pr-4">ジャンル検索</th>
  </tr>
  <% genres.each do |genre| %>
    <tr>
      <td class="text-center p-1"><%= genre.name %></td>
    </tr>
  <% end %>
</table>