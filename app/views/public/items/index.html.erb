<h1>商品一覧(全<%= @items.count %>件)</h1>

<% @items.each do |item| %>
  <%= link_to item_path(item.id) do %>
  <%= image_tag item.get_image %><% end %>
  <p><%= item.name %></p>
  <p>￥<%= item.add_tax_price.to_s(:delimited) %>円</p>
<% end %>
<%= paginate @items %><!--変わるかも-->