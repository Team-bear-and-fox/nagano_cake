<div class="container mt-5">
  <div class="row">
    <div class="col">
      <h5 class="font-weight-bold bg-light w-25 text-center">会員一覧</h5>
      <table class="table">
        <thead class="table-active">
          <tr>
          <th>会員ID</th>
          <th>氏名</th>
          <th>メールアドレス</th>
          <th>ステータス</th>
        </tr>
        </thead>
        <tbody>
          <% @customers.each do |customer| %>
          <tr>
            <td><%= customer.id %></td>
            <td>
              <%=link_to(admin_customer_path(customer.id)) do %>
                <%= customer.last_name %><%= customer.first_name %>
              <% end %>
            </td>
            <td><%= customer.email %></td>
            <td><%=  %>
              <%= "有効" if customer.is_active == true %>
              <%= "退会" if customer.is_active == false %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <%= paginate @customers %>
</div>

